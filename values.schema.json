{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "properties": {
    "deployAllReviewsVersions": {
      "description": "whether to deploy all three versions of the reviews service",
      "type": "boolean"
    },
    "deploySleep": {
      "description": "whether to also deploy the sleep app, which can be useful for making curl requests to the bookinfo endpoints",
      "type": "boolean"
    },
    "configureIngress": {
      "description": "whether to additionally configure ingress for bookinfo, by applying the bookinfo gateway and virtualservice resources",
      "type": "boolean"
    }
  },
  "required": [],
  "title": "Values",
  "type": "object"
}
